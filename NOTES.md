# NOTES FROM LABS FOR CODES' SYNTAX 

-example: 
    mkdir -p my_operator_proj/src
    mkdir -p my_operator_proj/classes          
    mkdir -p my_operator_proj/test
    cd my_operator_proj 
    -means: 
        You're now inside the `my_operator_proj` directory with these subfolders created:
        * src
        * classes
        * test 

